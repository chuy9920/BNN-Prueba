<form id="login" method="POST">
    <input type="text" id="user" name="usuario" placeholder="Usuario"> 
    <br><br>
    <input type="password" id="password" name="contrasenia" placeholder="Tu contraseña">
    <br><br>
    <input type="submit" value="Login">
</form>

<script>
    const formLogin = document.querySelector("#login");
    const formData = new FormData(formLogin);
    
    formLogin.addEventListener('submit', event => {
        event.preventDefault();
        var data = {
            usuario: document.getElementById('user').value,
            constrasenia: document.getElementById('password').value
        }

    fetch('/api/usuarios', {
        method: 'POST',
        body: JSON.stringify(data),
        headers: {
            'Content-Type': 'application/json'
        }
    })
    .then(res => res.json())
    .then(data => console.log(data))
        console.log(data);
        localStorage.setItem('logueado', 1)
        location.href = 'peliculas'
    });
</script>